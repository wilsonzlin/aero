'use strict';

/**
 * Bump when output schema changes in a backwards-incompatible way.
 * Consumers should treat this as an opaque, versioned contract.
 */
const SCHEMA_VERSION = 1;

module.exports = { SCHEMA_VERSION };

#pragma once

// Re-export the shared AeroGPU command stream writer implementation.
// (This path is kept for backwards compatibility with existing include paths.)
#include "../../common/aerogpu_cmd_stream_writer.h"


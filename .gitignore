# Benchmark outputs (generated by `bench/run` and `bench/compare`)
bench/results.json
bench/compare.md
bench/compare.json

# Local-only agent notes
scratchpad.md
handoff.md
.cargo-home/
.cargo_home/
/target-worker*/

# Python
__pycache__/
*.py[cod]

# Environment (secrets)
.env
.env.*
!**/.env.example
.envrc

# Credential / key files (secrets)
.npmrc
.netrc
*.key
*.pem
*.p12
*.pfx
!tools/win-blob-dump/examples/*.pem

# Local Helm values files (often contain secrets)
aero-values.yaml
aero-values*.yaml
values.local.yaml
values-local.yaml

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
*.tfvars
!**/*.tfvars.example
crash.log
crash.*.log
rustc-ice-*

# OS / editor files
.DS_Store
Thumbs.db
*.swp
*.swo
*.tmp
*~
.idea/
.vscode/
**/*.rs.bk

# Build outputs (common)
node_modules/
coverage/
dist/
build/
build-*/
out/
pkg/
target/
**/target/
pkg/
.vite/
.cache/
*.tsbuildinfo
*.proptest-regressions
proptest-regressions/
**/proptest-regressions/
*.wasm
*.wat
.cargo-home/

# NPM workspaces use a single repo-root lockfile; do not commit per-package lockfiles.
**/package-lock.json
!/package-lock.json
web/dist/
web/src/wasm/pkg-*/
bench/output/

# Source-controlled build tooling (not build outputs)
!drivers/aerogpu/build/
!drivers/aerogpu/build/**

# Build outputs produced by the AeroGPU build scripts
drivers/aerogpu/build/out/

# wasm-pack output (generated)
/web/pkg/
/web/pkg-dev/
/web/pkg-release/

# Driver workspace outputs (do not commit)
drivers/out/
drivers/tmp/
drivers/vendor/

# Playwright
.playwright/
playwright-report/
test-results/

# CI license reports (generated by `scripts/ci/check-*-licenses*`)
license-reports/

# Perf / bench outputs
bench/results/
bench/results.json
perf-results/

# Local test image cache (downloaded by scripts/prepare-*.sh)
test-images/*
!test-images/README.md

# Benchmark outputs (do not commit)
bench-results/

# Disk images / VM artifacts (do not commit)
*.iso
*.img
*.ima
*.vhd
*.vhdx
*.vmdk
*.vdi
*.qcow
*.qcow2
*.raw
*.dd
*.dsk
*.hdd
*.ova
*.ovf
*.bin

# Allow checked-in BIOS and tiny deterministic in-repo fixtures.
!assets/bios.bin
!test_images/boot_sectors/int_sanity.bin
!web/public/assets/security_headers_test.wasm
!tests/fixtures/bootsector.bin
!tests/fixtures/realmode_vbe_test.bin
!tests/fixtures/boot/*.img
!tests/fixtures/boot/*.bin
!tools/qemu_diff/boot/boot.bin
!crates/aero-d3d11/tests/fixtures/*.bin

# qemu_diff boot sector build byproducts (boot.bin is committed)
tools/qemu_diff/boot/boot.o
tools/qemu_diff/boot/boot.elf

# Local disk image directory used by `aero-storage-server` docker compose setups.
# Keep the directory itself tracked via `images/.gitkeep`.
/images/*
!/images/.gitkeep

# Windows installation / update packages (do not commit)
*.wim
*.esd
*.swm
*.cab
*.msu
*.msi

# Windows binaries/drivers (do not commit)
*.exe
*.dll
*.sys
*.drv
*.ocx
*.cpl
*.pdb

# Firmware / ROM dumps (do not commit)
*.rom
*.fd
*.efi

# OPFS / IndexedDB exports / local dumps
opfs/
opfs-dump/
opfs-dumps/
opfs-export/
opfs-exported/
indexeddb/
storage-dump/

# Captures / traces / dumps
captures/
traces/
dumps/
*.pcap
*.pcapng
*.etl
*.dmp
*.core
*.trace
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# cargo-fuzz
fuzz/artifacts/
fuzz/corpus/

# Aero Win7 virtio selftest build outputs (avoid clutter if building with MSVC locally)
drivers/windows7/tests/guest-selftest/**/Debug/
drivers/windows7/tests/guest-selftest/**/Release/
**/*.snap.new

# wasm-pack output (generated)
web/src/wasm/pkg-threaded/
web/src/wasm/pkg-single/
# wasm-pack microbench output (generated). We keep a tracked TS stub for typecheck.
web/src/wasm/aero_microbench/*
!web/src/wasm/aero_microbench/aero_microbench.ts
# Visual Studio / MSBuild
**/bin/
**/obj/
**/.vs/
**/*.user
**/*.suo
**/*.tlog
**/*.ipch
**/*.ilk
**/*.exp
**/*.lib

# Benchmark output (local runs)
backend/aero-gateway/bench/results*.json

# CMake (in-source builds)
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
Testing/

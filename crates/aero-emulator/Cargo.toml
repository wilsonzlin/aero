[package]
name = "aero-emulator"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]

[dev-dependencies]
aero-protocol = { path = "../../emulator/protocol" }

[[test]]
name = "vbe_smoke"
path = "../../tests/vbe_smoke.rs"

[package]
name = "conformance"
version = "0.1.0"
edition = "2021"

[dependencies]
iced-x86 = "1.21.0"
libc = "0.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]

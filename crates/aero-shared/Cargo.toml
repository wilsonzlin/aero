[package]
name = "aero-shared"
version = "0.1.0"
edition = "2021"

[features]
# Enables Loom-based concurrency testing for the publish protocol.
loom = ["dep:loom"]

[dependencies]
loom = { version = "0.7", optional = true }


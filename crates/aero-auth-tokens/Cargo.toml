[package]
name = "aero-auth-tokens"
version = "0.1.0"
edition = "2021"

[dependencies]
base64 = "0.22"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
subtle = "2.6"

[target.'cfg(not(target_arch = "wasm32"))'.dev-dependencies]
proptest = "1.5.0"

[package]
name = "aero-emulator"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
aero-protocol = { path = "../../emulator/protocol" }

[dev-dependencies]

[[test]]
name = "vbe_smoke"
path = "../../tests/vbe_smoke.rs"

[package]
name = "aero-devices"
version = "0.1.0"
edition = "2021"

[dependencies]
aero-platform = { path = "../platform" }
memory = { path = "../memory" }

[dev-dependencies]
aero-acpi = { path = "../aero-acpi" }

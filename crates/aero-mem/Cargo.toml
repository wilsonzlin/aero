[package]
name = "aero-mem"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]

[dev-dependencies]
criterion = "0.5"
proptest = "1.5"

[[bench]]
name = "bulk"
harness = false


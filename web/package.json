{
  "name": "aero-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.11.0 <23"
  },
  "scripts": {
    "check:node": "node ../scripts/check-node-version.mjs",
    "build:wasm:microbench": "wasm-pack build --target web --release --no-opt --out-dir src/wasm/aero_microbench ../crates/aero-microbench --locked",
    "wasm:build:threaded": "node ./scripts/build_wasm.mjs threaded",
    "wasm:build:single": "node ./scripts/build_wasm.mjs single",
    "wasm:ensure": "node ./scripts/ensure_wasm.mjs",
    "wasm:build": "npm run wasm:build:single && npm run wasm:build:threaded",
    "build:wasm": "npm run build:wasm:microbench && npm run wasm:ensure",
    "predev": "npm run check:node && npm run build:wasm",
    "dev": "vite",
    "prebuild": "npm run check:node && npm run build:wasm",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "vite preview",
    "typecheck": "tsc -p tsconfig.json",
    "pretest": "npm run check:node",
    "test": "vitest run",
    "pretest:unit": "npm run check:node",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "pretest:unit:coverage": "npm run check:node",
    "test:unit:coverage": "vitest run --coverage",
    "wasm:build:dev": "node ./scripts/build_wasm.mjs single dev",
    "wasm:build:release": "npm run wasm:build",
    "wasm:size": "node ./scripts/wasm-size.mjs"
  }
}

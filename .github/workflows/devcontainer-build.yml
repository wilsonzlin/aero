name: Dev Container (build)

on:
  pull_request:
    paths:
      - ".devcontainer/**"
      - "scripts/agent-env.sh"
      - "rust-toolchain.toml"
      - "package.json"
      - "web/package.json"
  push:
    branches: [main]
    paths:
      - ".devcontainer/**"
      - "scripts/agent-env.sh"
      - "rust-toolchain.toml"
      - "package.json"
      - "web/package.json"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build dev container image
        run: docker build -f .devcontainer/Dockerfile -t aero-devcontainer .


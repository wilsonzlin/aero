# Required
S3_BUCKET=aero-images
AWS_REGION=us-east-1

# S3 credentials (for AWS, use your preferred method; for MinIO see docker-compose.minio.yml)
# AWS_ACCESS_KEY_ID=minioadmin
# AWS_SECRET_ACCESS_KEY=minioadmin

# Optional: S3-compatible / MinIO
# S3_ENDPOINT=http://127.0.0.1:9000
# S3_FORCE_PATH_STYLE=true

# CloudFront (required for /stream-url in cookie/url mode)
# CLOUDFRONT_DOMAIN=dxxxxx.cloudfront.net
# CLOUDFRONT_KEY_PAIR_ID=KXXXXXXXXXXXX
# CLOUDFRONT_PRIVATE_KEY_PEM=/path/to/cloudfront-private-key.pem
# # Optional: cookie auth tuning/hardening (only relevant for CLOUDFRONT_AUTH_MODE=cookie)
# # CLOUDFRONT_COOKIE_DOMAIN=.example.com
# # CLOUDFRONT_COOKIE_SAMESITE=None
# # CLOUDFRONT_COOKIE_PARTITIONED=false

# Service
IMAGE_BASE_PATH=/images
AUTH_MODE=dev
PORT=3000

# Multipart upload tuning
# MULTIPART_PART_SIZE_BYTES=67108864

# Local dev range-proxy CORS
# CORS_ALLOW_ORIGIN=http://localhost:5173

# Disk streaming CORP (COEP defence-in-depth; see docs/16-disk-image-streaming-auth.md)
# CROSS_ORIGIN_RESOURCE_POLICY=same-site

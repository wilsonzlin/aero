apiVersion: v2
name: aero-gateway
description: Deploy the Aero gateway backend (HTTP + WebSocket /tcp) with COOP/COEP/CSP security headers at the ingress.
type: application
version: 0.1.0
appVersion: "0.0.0"
